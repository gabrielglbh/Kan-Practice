import 'package:flutter/material.dart';

class MarketListRating extends StatelessWidget {
  const MarketListRating({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
